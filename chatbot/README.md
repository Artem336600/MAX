# Многофункциональный бот для MAKS

Профессиональный масштабируемый бот для мессенджера MAKS с модульной архитектурой, кнопками, командами и персональной статистикой. Написан на Python с использованием библиотеки `aiomax`.

> 📁 **Две версии бота:**
> - `max_bot.py` - простая монолитная версия (для обучения)
> - `bot.py` - модульная версия с профессиональной архитектурой (для production)

## Возможности

- 🎮 **Кликер** - интерактивная игра с подсчетом кликов и мотивирующими сообщениями
- 🎲 **Генератор случайных чисел** - получай случайные числа от 1 до 100
- 📊 **Персональная статистика** - отслеживание всех действий пользователя (FSM)
- 💬 **Эхо-режим** - бот повторяет ваши сообщения с разными вариантами ответов
- 🎯 **Интерактивное меню** - удобная навигация с помощью кнопок
- ⌨️ **Команды** - полный набор команд для управления ботом

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Настройка

1. Создайте бота через [@MasterBot](https://max.ru/masterbot) в мессенджере MAKS:
   - Отправьте `/create`
   - Придумайте уникальное имя пользователя (больше 11 символов, заканчивается на `_bot` или `bot`)
   - Укажите отображаемое имя бота
   - Скопируйте полученный токен

2. **Для простой версии (`max_bot.py`):**
   - Откройте файл `max_bot.py`
   - Замените `YOUR_BOT_TOKEN_HERE` на ваш токен

3. **Для модульной версии (`bot.py`):**
   - Скопируйте `.env.example` в `.env`
   - Откройте `.env` и укажите ваш токен:
     ```
     BOT_TOKEN=ваш_токен_здесь
     ```
   - Или измените токен в `config.py`

## Запуск

**Простая версия:**
```bash
python max_bot.py
```

**Модульная версия (рекомендуется):**
```bash
python bot.py
```

## Использование

1. Найдите вашего бота в мессенджере MAKS
2. Нажмите кнопку "Начать"
3. Используйте интерактивное меню с кнопками или команды

### Доступные команды

- `/menu` - открыть главное меню
- `/clicker` - запустить кликер
- `/stats` - посмотреть свою статистику
- `/help` - справка по командам

### Интерактивные возможности

- **Главное меню** - выбирайте действия с помощью кнопок
- **Кликер** - нажимайте на кнопку и набирайте очки, получайте достижения на 10, 50, 100 и каждые 100 кликов
- **Случайное число** - генерируйте случайные числа с разными эмодзи в зависимости от результата
- **Статистика** - отслеживайте количество кликов, сгенерированных чисел и отправленных сообщений
- **Эхо-режим** - просто пишите сообщения, и бот будет отвечать разными вариантами

## Структура проекта

```
OPTIMIZATION/
├── bot.py                 # Главный файл (модульная версия)
├── max_bot.py            # Простая версия (монолитная)
├── config.py             # Конфигурация и настройки
├── requirements.txt      # Зависимости
├── .env.example         # Пример переменных окружения
├── .gitignore           # Игнорируемые файлы
├── README.md            # Документация
├── ARCHITECTURE.md      # Описание архитектуры
│
├── handlers/            # Обработчики событий
│   ├── commands.py      # Команды (/menu, /stats и т.д.)
│   ├── callbacks.py     # Callback кнопок
│   └── messages.py      # Текстовые сообщения
│
├── keyboards/           # Клавиатуры с кнопками
│   └── main.py         # Основные клавиатуры
│
├── database/           # Работа с данными
│   └── user_data.py    # Модель данных пользователя
│
└── utils/              # Утилиты
    ├── formatters.py   # Форматирование сообщений
    └── achievements.py # Система достижений
```

Подробное описание архитектуры см. в [ARCHITECTURE.md](ARCHITECTURE.md)

## Технические особенности

### Модульная архитектура
- **Разделение ответственности** - каждый модуль отвечает за свою область
- **Расширяемость** - легко добавлять новые функции
- **Поддерживаемость** - код легко читать и изменять
- **Тестируемость** - модули можно тестировать независимо

### Технологии
- **FSM (Finite State Machine)** - для хранения персональных данных каждого пользователя
- **Callback кнопки** - интерактивные кнопки с обработчиками событий
- **Keyboard Builder** - построение клавиатур с кнопками
- **Команды** - обработка текстовых команд через декораторы
- **Markdown форматирование** - красивое оформление сообщений
- **Dataclasses** - типизированные структуры данных
- **Type hints** - аннотации типов для безопасности

## Дополнительная информация

Бот основан на статье: https://habr.com/ru/articles/934140/

Документация библиотеки aiomax: https://pypi.org/project/aiomax/
