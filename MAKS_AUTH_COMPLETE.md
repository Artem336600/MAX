# ✅ MAKS Авторизация - ГОТОВО!

## 🎯 Что реализовано

### 1. Генерация учетных данных в боте
- ✅ Утилита `credentials.py` для генерации логина и пароля
- ✅ Логин: `eidos_abc123` (уникальный на основе MAKS ID)
- ✅ Пароль: 8 символов (буквы + цифры)
- ✅ Хеширование паролей SHA-256

### 2. Обновленная команда `/start`
- ✅ Автоматическое создание пользователя
- ✅ Выдача логина и пароля при первом запуске
- ✅ Показ логина при повторном запуске
- ✅ Ссылка на сайт

### 3. Команда `/credentials`
- ✅ Показывает текущий логин
- ✅ Напоминает о пароле
- ✅ Ссылка на сайт

### 4. API для авторизации
- ✅ Endpoint: `POST /api/v1/maks-auth/login`
- ✅ Проверка логина и пароля
- ✅ Выдача JWT токена
- ✅ Endpoint: `GET /api/v1/maks-auth/check-username/{username}`

### 5. Обновленная модель User
- ✅ Поле `maks_id` - ID пользователя в MAKS
- ✅ Поле `maks_username` - логин для входа на сайт
- ✅ Индексы для быстрого поиска

### 6. Страница входа через MAKS
- ✅ `/maks-login` - красивая форма входа
- ✅ Инструкция как получить учетные данные
- ✅ Ссылка на обычный вход

## 🚀 Как использовать

### Для пользователя:

**1. Получить учетные данные:**
```
Пользователь → MAKS → @t33_hakaton_bot
Команда: /start

Бот ответит:
🔑 Твои учетные данные для входа на сайт:
• Логин: eidos_abc123
• Пароль: Xy7mK2nQ

⚠️ Сохрани эти данные!
🌐 Сайт: http://localhost:3000
```

**2. Войти на сайт:**
```
1. Открыть http://localhost:3000/maks-login
2. Ввести логин: eidos_abc123
3. Ввести пароль: Xy7mK2nQ
4. Нажать "Войти"
```

**3. Посмотреть логин:**
```
Команда: /credentials

Бот ответит:
🔑 Твои учетные данные:
• Логин: eidos_abc123
• Пароль: (был выдан при первом /start)
```

### Для разработчика:

**1. Запустить бота:**
```powershell
$env:DEEPSEEK_API_KEY="sk-..."
cd chatbot
python bot_ai.py
```

**2. Запустить backend:**
```bash
cd Web/backend
python main.py
```

**3. Запустить frontend:**
```bash
cd Web/frontend
npm run dev
```

## 📊 Архитектура

```
┌─────────────────┐
│  MAKS Messenger │
│                 │
│  /start         │──┐
│  /credentials   │  │
└─────────────────┘  │
                     │
                     ▼
              ┌──────────────┐
              │   MAKS Bot   │
              │              │
              │ • Генерация  │
              │   логина     │
              │ • Генерация  │
              │   пароля     │
              │ • Сохранение │
              │   в БД       │
              └──────────────┘
                     │
                     ▼
              ┌──────────────┐
              │  Web Backend │
              │              │
              │ • User model │
              │   + maks_id  │
              │   + username │
              │ • MAKS Auth  │
              │   API        │
              └──────────────┘
                     │
                     ▼
              ┌──────────────┐
              │ Web Frontend │
              │              │
              │ /maks-login  │
              │              │
              └──────────────┘
```

## 🔐 Безопасность

### Хеширование паролей:
```python
import hashlib

def hash_password(password: str) -> str:
    return hashlib.sha256(password.encode()).hexdigest()
```

### Генерация логина:
```python
def generate_username(maks_id: int) -> str:
    hash_part = hashlib.md5(str(maks_id).encode()).hexdigest()[:6]
    return f"eidos_{hash_part}"
```

### Генерация пароля:
```python
import secrets
import string

def generate_simple_password(length: int = 8) -> str:
    alphabet = string.ascii_letters + string.digits
    return ''.join(secrets.choice(alphabet) for _ in range(length))
```

## 📝 API Endpoints

### POST /api/v1/maks-auth/login
Вход через MAKS credentials

**Request:**
```json
{
  "username": "eidos_abc123",
  "password": "Xy7mK2nQ"
}
```

**Response:**
```json
{
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "token_type": "bearer",
  "user": {
    "id": "uuid",
    "name": "Пользователь",
    "email": "maks_123@bot.local",
    "role": "user",
    "maks_username": "eidos_abc123"
  }
}
```

### GET /api/v1/maks-auth/check-username/{username}
Проверить существование логина

**Response:**
```json
{
  "exists": true,
  "username": "eidos_abc123"
}
```

## 🗄️ База данных

### Обновленная таблица users:
```sql
CREATE TABLE users (
    id VARCHAR PRIMARY KEY,
    email VARCHAR UNIQUE NOT NULL,
    password_hash VARCHAR NOT NULL,
    name VARCHAR NOT NULL,
    role VARCHAR NOT NULL,
    maks_id INTEGER UNIQUE,          -- NEW
    maks_username VARCHAR UNIQUE,    -- NEW
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

CREATE INDEX idx_users_maks_id ON users(maks_id);
CREATE INDEX idx_users_maks_username ON users(maks_username);
```

## 🎯 Преимущества

### Для пользователя:
- ✅ Не нужно регистрироваться на сайте
- ✅ Логин и пароль выдаются автоматически
- ✅ Один аккаунт для бота и сайта
- ✅ Данные синхронизируются

### Для системы:
- ✅ Единая база пользователей
- ✅ Автоматическая регистрация
- ✅ Безопасное хранение паролей
- ✅ Уникальные логины

## 📱 Примеры использования

### Сценарий 1: Новый пользователь
```
1. Пользователь → MAKS → /start
2. Бот создает аккаунт
3. Бот выдает логин: eidos_abc123
4. Бот выдает пароль: Xy7mK2nQ
5. Пользователь → Сайт → /maks-login
6. Вход успешен!
```

### Сценарий 2: Существующий пользователь
```
1. Пользователь → MAKS → /start
2. Бот находит аккаунт
3. Бот показывает логин: eidos_abc123
4. Пользователь → MAKS → /credentials
5. Бот напоминает логин
```

### Сценарий 3: Работа с ботом и сайтом
```
1. Пользователь → MAKS → "Я спал 8 часов"
2. Бот создает запись в БД
3. Пользователь → Сайт → /dashboard/sleep
4. Видит свою запись о сне!
```

## ✅ Чеклист

- [x] Создана утилита генерации credentials
- [x] Обновлена команда /start
- [x] Добавлена команда /credentials
- [x] Создан API endpoint для авторизации
- [x] Обновлена модель User
- [x] Создана страница /maks-login
- [x] Зарегистрирован router в main.py
- [x] Документация готова

## 🚀 Запуск

```bash
# 1. Запустить бота
cd chatbot
$env:DEEPSEEK_API_KEY="sk-..."
python bot_ai.py

# 2. Запустить backend
cd Web/backend
python main.py

# 3. Запустить frontend
cd Web/frontend
npm run dev

# 4. Протестировать
# - MAKS: /start
# - Сайт: http://localhost:3000/maks-login
```

---

**ГОТОВО! Пользователи могут входить на сайт через MAKS!** 🎉✨
