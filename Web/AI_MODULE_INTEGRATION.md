# Интеграция ИИ с модулями

## 📋 Обзор

ИИ-ассистент в Eidos автоматически понимает какие модули активны у пользователя и может с ними взаимодействовать через систему функций.

## 🔄 Как это работает

### 1. Определение активных модулей

При каждом запросе к ИИ система:

```python
# backend/core/module_integration.py
async def get_user_modules(self, user_id: str) -> List[Module]:
    """Получить активные модули пользователя"""
    result = await self.db.execute(
        select(Module)
        .join(UserModule)
        .where(
            UserModule.user_id == user_id,
            UserModule.enabled == True  # Только включенные модули
        )
    )
    return result.scalars().all()
```

**Критерии активности модуля:**
- ✅ Модуль установлен пользователем (`UserModule` запись существует)
- ✅ Модуль включен (`UserModule.enabled == True`)
- ✅ Модуль опубликован (`Module.status == 'public'`) ИЛИ это черновик автора

### 2. Построение System Prompt

ИИ получает контекст о доступных модулях:

```python
async def build_system_prompt(self, user_id: str, user_name: str) -> str:
    modules = await self.get_user_modules(user_id)
    
    prompt = f"""Ты персональный ИИ-ассистент пользователя {user_name}.

У пользователя установлены следующие модули:
"""
    
    # Встроенные модули (всегда доступны)
    prompt += "- Календарь: управление событиями и напоминаниями\n"
    prompt += "- Sleep Tracker: отслеживание качества сна\n"
    prompt += "- Habit Tracker: трекер привычек и целей\n"
    prompt += "- Finance Manager: управление финансами и бюджетом\n"
    
    # Установленные модули из маркетплейса
    for module in modules:
        manifest = module.manifest or {}
        description = manifest.get('description', module.description)
        prompt += f"- {module.name}: {description}\n"
```

### 3. Регистрация функций модулей

Каждый модуль может предоставлять функции для ИИ через манифест:

```json
{
  "name": "Workout Analyzer",
  "description": "Анализ тренировок",
  "functions": [
    {
      "name": "analyze_workout",
      "description": "Проанализировать тренировку пользователя",
      "parameters": {
        "workout_data": {
          "type": "object",
          "description": "Данные о тренировке"
        }
      },
      "endpoint": "/analyze"
    }
  ]
}
```

### 4. Преобразование в OpenAI Functions

Функции модулей конвертируются в формат OpenAI:

```python
def to_openai_function(self) -> Dict:
    return {
        "name": self.name,
        "description": self.description,
        "parameters": {
            "type": "object",
            "properties": self.parameters,
            "required": list(self.parameters.keys())
        }
    }
```

### 5. Выполнение функций

Когда ИИ решает вызвать функцию:

```python
# backend/api/chat.py
async def execute_function(name: str, args: dict):
    return await context_builder.execute_function(name, args, current_user.id)

# ИИ получает функции и может их вызывать
ai_content = await ai_engine.chat_with_functions(
    messages,
    functions,
    execute_function
)
```

## 🎯 Встроенные модули

### Календарь
- `create_calendar_event` - создать событие
- `get_calendar_events` - получить события

### Sleep Tracker
- `get_sleep_stats` - статистика сна

### Habit Tracker
- `get_habits` - список привычек
- `get_habit_stats` - статистика привычек

### Finance Manager
- `get_finance_stats` - финансовая статистика
- `get_transactions` - список транзакций

## 📦 Пользовательские модули

### Как модуль добавляет функции

1. **В манифесте модуля** (`manifest.json`):
```json
{
  "functions": [
    {
      "name": "my_custom_function",
      "description": "Что делает эта функция",
      "parameters": {
        "param1": {
          "type": "string",
          "description": "Описание параметра"
        }
      },
      "endpoint": "/my-endpoint"
    }
  ]
}
```

2. **В HTTP сервере модуля**:
```python
@app.post("/my-endpoint")
async def my_endpoint(request: Request):
    data = await request.json()
    # Обработка запроса
    return {"result": "success"}
```

3. **ИИ автоматически получает доступ** к этой функции!

## 🔐 Безопасность

### Проверка прав доступа

```python
async def execute_function(
    self,
    function_name: str,
    arguments: Dict[str, Any],
    user_id: str
) -> Any:
    # 1. Получить доступные функции пользователя
    functions = await self.get_available_functions(user_id)
    
    # 2. Найти функцию
    func = None
    for f in functions:
        if f.name == function_name:
            func = f
            break
    
    # 3. Если функция не найдена - ошибка
    if not func:
        raise ValueError(f"Function {function_name} not found")
    
    # 4. Выполнить функцию
    # Здесь происходит вызов модуля через module_proxy
```

**Важно:** ИИ может вызывать только те функции, которые предоставлены **активными** модулями пользователя.

## 🔄 Жизненный цикл запроса

```
1. Пользователь отправляет сообщение
   ↓
2. Система загружает активные модули пользователя
   ↓
3. Строится system prompt с описанием модулей
   ↓
4. Собираются все доступные функции (встроенные + модули)
   ↓
5. Функции передаются в OpenAI API
   ↓
6. ИИ анализирует запрос и решает какие функции вызвать
   ↓
7. Система выполняет вызовы функций
   ↓
8. ИИ получает результаты и формирует ответ
   ↓
9. Ответ отправляется пользователю
```

## 📊 Пример взаимодействия

**Пользователь:** "Проанализируй мою последнюю тренировку"

**Система:**
1. Проверяет активные модули → находит "Workout Analyzer"
2. Видит функцию `analyze_workout`
3. Передает её ИИ

**ИИ:**
1. Понимает что нужно использовать `analyze_workout`
2. Вызывает функцию
3. Получает результат анализа
4. Формирует понятный ответ пользователю

**Ответ:** "🏋️ Анализ твоей тренировки:
- Объем: 12 тонн
- Интенсивность: высокая
- Рекомендация: добавь день отдыха"

## 🛠️ Управление модулями

### Включение/отключение модулей

Пользователь может управлять модулями через UI:
- **Модули → Управление**
- Включить/отключить любой модуль
- Изменения сразу влияют на доступные функции ИИ

### Черновики модулей

Модули в статусе `draft`:
- ✅ Видны только автору
- ✅ Доступны ИИ автора для тестирования
- ❌ Не видны другим пользователям

## 🔮 Будущие улучшения

1. **Кэширование модулей** - для быстрого доступа
2. **Приоритеты функций** - какие функции вызывать в первую очередь
3. **Контекст модулей** - модули могут предоставлять дополнительный контекст
4. **Уведомления от модулей** - модули могут инициировать сообщения
5. **Межмодульное взаимодействие** - модули могут вызывать функции друг друга

## 📝 Резюме

**ИИ понимает активные модули через:**
1. ✅ Запрос к БД (`UserModule.enabled == True`)
2. ✅ Чтение манифестов модулей
3. ✅ Построение system prompt с описанием
4. ✅ Регистрацию функций в OpenAI API
5. ✅ Динамическое выполнение через module_proxy

**Все автоматически!** Разработчик модуля просто добавляет функции в манифест, и ИИ сразу может их использовать.
