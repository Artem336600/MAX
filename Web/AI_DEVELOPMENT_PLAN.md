# План разработки Eidos - Пошаговый для ИИ

## 🎯 Цель
Создать платформу модульного ИИ-ассистента с маркетплейсом модулей на Python.

---

## Этап 1: Базовая инфраструктура (Неделя 1-2)

### Шаг 1.1: Настройка проекта
```bash
# Создать структуру
mkdir -p backend frontend sdk/python docs
```

**Backend:**
- FastAPI + SQLAlchemy (async)
- PostgreSQL (prod) / SQLite (dev)
- JWT аутентификация
- CORS настройка

**Frontend:**
- Next.js 14 (App Router)
- TailwindCSS + shadcn/ui
- Zustand (state management)
- Axios (API client)

**Файлы для создания:**
```
backend/
├── main.py                 # FastAPI app
├── core/
│   ├── database.py        # SQLAlchemy setup
│   ├── security.py        # JWT, bcrypt
│   └── config.py          # Settings
├── models/
│   └── user.py            # User model
└── api/
    └── auth.py            # Auth endpoints

frontend/
├── src/
│   ├── app/
│   │   ├── (auth)/
│   │   │   ├── login/page.tsx
│   │   │   └── register/page.tsx
│   │   └── (dashboard)/
│   │       └── layout.tsx
│   ├── lib/
│   │   └── api.ts         # Axios instance
│   └── store/
│       └── authStore.ts   # Zustand auth
```

**Задачи:**
1. Создать FastAPI приложение
2. Настроить SQLAlchemy с async
3. Реализовать User модель
4. Создать auth endpoints (register, login, me)
5. Настроить JWT токены
6. Создать Next.js приложение
7. Реализовать страницы login/register
8. Настроить Axios с interceptors
9. Создать auth store (Zustand)

**Проверка:**
- ✅ Можно зарегистрироваться
- ✅ Можно войти
- ✅ Токен сохраняется
- ✅ Защищённые роуты работают

---

## Этап 2: Базовый модуль - Календарь (Неделя 3-4)

### Шаг 2.1: Backend календаря

**Модели:**
```python
# models/calendar.py
class CalendarEvent(Base):
    id: str
    user_id: str
    title: str
    description: str
    start_time: datetime
    end_time: datetime
    all_day: bool
    recurrence: str  # daily, weekly, monthly
    reminder_minutes: int
    color: str
    module_id: str  # какой модуль создал
```

**API endpoints:**
```python
# api/calendar.py
POST   /calendar/events          # Создать событие
GET    /calendar/events          # Список событий
GET    /calendar/events/{id}     # Получить событие
PUT    /calendar/events/{id}     # Обновить
DELETE /calendar/events/{id}     # Удалить
GET    /calendar/events/range    # События за период
```

**Задачи:**
1. Создать модель CalendarEvent
2. Реализовать CRUD API
3. Добавить фильтрацию по датам
4. Реализовать повторяющиеся события
5. Добавить напоминания

### Шаг 2.2: Frontend календаря

**Компоненты:**
```typescript
// components/calendar/
├── CalendarView.tsx       // Главный компонент
├── MonthView.tsx          // Вид месяца
├── WeekView.tsx           // Вид недели
├── DayView.tsx            // Вид дня
├── EventCard.tsx          // Карточка события
└── EventModal.tsx         // Модальное окно создания
```

**Страница:**
```typescript
// app/(dashboard)/calendar/page.tsx
- Переключение видов (месяц/неделя/день)
- Создание событий
- Редактирование событий
- Удаление событий
```

**Задачи:**
1. Создать CalendarView компонент
2. Реализовать MonthView (сетка дней)
3. Реализовать WeekView (почасовое)
4. Реализовать DayView (детальное)
5. Создать EventModal для создания/редактирования
6. Добавить drag & drop (опционально)
7. Реализовать цветовые метки

**Проверка:**
- ✅ Можно создать событие
- ✅ События отображаются в календаре
- ✅ Можно переключать виды
- ✅ Можно редактировать/удалять события

---

## Этап 3: Чат с ИИ (базовый) (Неделя 5-6)

### Шаг 3.1: Backend чата

**Модели:**
```python
# models/chat.py
class Conversation(Base):
    id: str
    user_id: str
    title: str
    created_at: datetime

class Message(Base):
    id: str
    conversation_id: str
    role: str  # user, assistant, system
    content: str
    function_call: dict  # для function calling
    created_at: datetime
```

**API endpoints:**
```python
# api/chat.py
POST   /chat/message            # Отправить сообщение
GET    /chat/conversations      # Список диалогов
GET    /chat/{id}/messages      # История сообщений
POST   /chat/conversations      # Новый диалог
DELETE /chat/{id}               # Удалить диалог
```

**OpenAI интеграция:**
```python
# core/ai.py
from openai import AsyncOpenAI

class AIEngine:
    def __init__(self):
        self.client = AsyncOpenAI(api_key=settings.OPENAI_API_KEY)
        self.model = "gpt-4-turbo-preview"
    
    async def chat(self, messages: list, functions: list = None):
        response = await self.client.chat.completions.create(
            model=self.model,
            messages=messages,
            functions=functions,
            function_call="auto"
        )
        return response
    
    def build_system_prompt(self, user_context: dict):
        """Создать system prompt с контекстом пользователя"""
        return f"""
        Ты персональный ИИ-ассистент пользователя {user_context['name']}.
        
        У пользователя установлены модули:
        - Календарь: управление событиями и напоминаниями
        
        Доступные функции:
        - create_calendar_event: создать событие в календаре
        - get_calendar_events: получить события
        
        Отвечай на русском языке, будь дружелюбным и полезным.
        """
```

**Functions для ИИ:**
```python
# api/ai_functions.py
CALENDAR_FUNCTIONS = [
    {
        "name": "create_calendar_event",
        "description": "Создать событие в календаре",
        "parameters": {
            "type": "object",
            "properties": {
                "title": {"type": "string"},
                "start_time": {"type": "string"},
                "end_time": {"type": "string"},
                "description": {"type": "string"},
                "reminder_minutes": {"type": "integer"}
            },
            "required": ["title", "start_time"]
        }
    },
    {
        "name": "get_calendar_events",
        "description": "Получить события из календаря",
        "parameters": {
            "type": "object",
            "properties": {
                "start_date": {"type": "string"},
                "end_date": {"type": "string"}
            }
        }
    }
]

async def execute_function(function_name: str, arguments: dict, user_id: str):
    """Выполнить функцию, вызванную ИИ"""
    if function_name == "create_calendar_event":
        return await create_calendar_event(user_id, **arguments)
    elif function_name == "get_calendar_events":
        return await get_calendar_events(user_id, **arguments)
```

**Задачи:**
1. Создать модели Conversation и Message
2. Реализовать chat API endpoints
3. Интегрировать OpenAI API
4. Создать AIEngine класс
5. Реализовать system prompt builder
6. Добавить functions для календаря
7. Реализовать execute_function
8. Добавить обработку function calling

### Шаг 3.2: Frontend чата

**Компоненты:**
```typescript
// components/chat/
├── ChatInterface.tsx      // Главный интерфейс
├── MessageList.tsx        // Список сообщений
├── MessageBubble.tsx      // Пузырь сообщения
├── ChatInput.tsx          // Поле ввода
└── TypingIndicator.tsx    // Индикатор печати
```

**Страница:**
```typescript
// app/(dashboard)/chat/page.tsx
- Отображение сообщений
- Отправка сообщений
- Автоскролл вниз
- Индикатор печати
- История диалогов (sidebar)
```

**Задачи:**
1. Создать ChatInterface компонент
2. Реализовать MessageList с автоскроллом
3. Создать MessageBubble (user/assistant стили)
4. Реализовать ChatInput с отправкой
5. Добавить TypingIndicator
6. Реализовать загрузку истории
7. Добавить создание нового диалога
8. Реализовать sidebar с диалогами

**Проверка:**
- ✅ Можно отправить сообщение
- ✅ ИИ отвечает
- ✅ ИИ может создать событие в календаре
- ✅ История сохраняется
- ✅ Можно создать новый диалог

---

## Этап 4: Система модулей (Неделя 7-10)

### Шаг 4.1: Модели модулей

**Backend модели:**
```python
# models/module.py
class Module(Base):
    id: str
    name: str
    description: str
    author_id: str
    version: str
    manifest: dict  # YAML манифест
    code: str  # Python код
    api_key: str  # Уникальный ключ модуля
    status: str  # draft, public, private
    rating: float
    installs: int
    created_at: datetime

class UserModule(Base):
    id: str
    user_id: str
    module_id: str
    enabled: bool
    config: dict
    installed_at: datetime

class ModulePermission(Base):
    id: str
    module_id: str
    permission_type: str  # database, ai, notifications, etc
    granted: bool
```

**Задачи:**
1. Создать модели Module, UserModule, ModulePermission
2. Реализовать CRUD API для модулей
3. Добавить генерацию API ключа
4. Реализовать установку/удаление модулей
5. Добавить систему разрешений

### Шаг 4.2: Python SDK

**Структура SDK:**
```python
# sdk/python/eidos_sdk/
├── __init__.py
├── module.py          # EidosModule базовый класс
├── client.py          # API клиент
├── decorators.py      # @webhook, @function
└── types.py           # Типы данных
```

**Базовый класс модуля:**
```python
# eidos_sdk/module.py
class EidosModule:
    def __init__(self, name: str, version: str, api_key: str):
        self.name = name
        self.version = version
        self.api_key = api_key
        self.client = EidosClient(api_key)
    
    # Хуки
    def on_install(self, user_id: str): pass
    def on_uninstall(self, user_id: str): pass
    def on_message(self, message: str, user_id: str): pass
    
    # API методы
    async def get_user_data(self, user_id: str, key: str): pass
    async def set_user_data(self, user_id: str, key: str, value): pass
    async def notify(self, user_id: str, message: str): pass
```

**Задачи:**
1. Создать структуру SDK
2. Реализовать EidosModule базовый класс
3. Создать EidosClient для API
4. Добавить декораторы @webhook, @function
5. Реализовать типы данных
6. Написать документацию
7. Создать примеры модулей
8. Опубликовать в PyPI (опционально)

### Шаг 4.3: Маркетплейс модулей

**Frontend страницы:**
```typescript
// app/(dashboard)/modules/
├── page.tsx              // Список модулей
├── [id]/page.tsx         // Страница модуля
├── new/page.tsx          // Создание модуля
└── my/page.tsx           // Мои модули
```

**Компоненты:**
```typescript
// components/modules/
├── ModuleCard.tsx        // Карточка модуля
├── ModuleGrid.tsx        // Сетка модулей
├── ModuleFilters.tsx     // Фильтры
├── ModuleDetails.tsx     // Детали модуля
└── InstallButton.tsx     // Кнопка установки
```

**Задачи:**
1. Создать страницу списка модулей
2. Реализовать фильтры (категории, рейтинг)
3. Создать страницу модуля (детали, отзывы)
4. Реализовать установку модуля
5. Создать страницу "Мои модули"
6. Реализовать создание модуля
7. Добавить редактор модуля
8. Показать API ключ модуля

**Проверка:**
- ✅ Можно просматривать модули
- ✅ Можно установить модуль
- ✅ Можно создать свой модуль
- ✅ Получен API ключ

---

## Этап 5: Интеграция модулей с ИИ (Неделя 11-12)

### Шаг 5.1: Контекст модулей для ИИ

**Сбор контекста:**
```python
# core/ai_context.py
class AIContextBuilder:
    async def build_context(self, user_id: str):
        """Собрать контекст всех модулей пользователя"""
        
        # Получить установленные модули
        modules = await get_user_modules(user_id)
        
        context = {
            "user_id": user_id,
            "modules": []
        }
        
        for module in modules:
            # Получить манифест модуля
            manifest = module.manifest
            
            # Получить краткую сводку данных
            data_summary = await get_module_data_summary(
                module.id, user_id
            )
            
            context["modules"].append({
                "id": module.id,
                "name": module.name,
                "description": manifest["description"],
                "functions": manifest.get("ai", {}).get("functions", []),
                "data_summary": data_summary
            })
        
        return context
```

**Динамический system prompt:**
```python
def build_system_prompt(context: dict):
    prompt = f"Ты персональный ИИ-ассистент.\n\n"
    prompt += "У пользователя установлены модули:\n\n"
    
    for module in context["modules"]:
        prompt += f"{module['name']}:\n"
        prompt += f"- {module['description']}\n"
        
        if module.get("data_summary"):
            prompt += f"- Последние данные: {module['data_summary']}\n"
        
        prompt += f"- Функции: {', '.join([f['name'] for f in module['functions']])}\n\n"
    
    return prompt
```

**Задачи:**
1. Создать AIContextBuilder
2. Реализовать сбор контекста модулей
3. Добавить data_summary для модулей
4. Реализовать динамический system prompt
5. Добавить функции всех модулей в ИИ
6. Реализовать маршрутизацию вызовов функций
7. Добавить обработку ошибок

### Шаг 5.2: Module API для интеграции

**Публичный API модулей:**
```python
# api/module_api.py
class ModuleAPI:
    @staticmethod
    async def call(module_id: str, endpoint: str, user_id: str, **kwargs):
        """Вызвать публичный endpoint другого модуля"""
        
        # Проверить разрешения
        if not has_permission(requesting_module, module_id, user_id):
            raise PermissionError()
        
        # Вызвать endpoint модуля
        module = await get_module(module_id)
        result = await execute_module_endpoint(
            module, endpoint, user_id, **kwargs
        )
        
        return result
```

**Задачи:**
1. Создать ModuleAPI класс
2. Реализовать call метод
3. Добавить проверку разрешений
4. Реализовать execute_module_endpoint
5. Добавить в SDK
6. Создать примеры интеграции

**Проверка:**
- ✅ ИИ видит все модули
- ✅ ИИ может вызывать функции модулей
- ✅ Модули могут вызывать друг друга
- ✅ Разрешения работают

---

## Этап 6: Dashboard и виджеты (Неделя 13-14)

### Шаг 6.1: Система виджетов

**Backend:**
```python
# models/widget.py
class Widget(Base):
    id: str
    user_id: str
    module_id: str
    widget_type: str  # small, medium, large
    position: int
    config: dict
    enabled: bool
```

**API:**
```python
POST   /widgets              # Добавить виджет
GET    /widgets              # Список виджетов
PUT    /widgets/{id}         # Обновить
DELETE /widgets/{id}         # Удалить
PUT    /widgets/reorder      # Изменить порядок
```

**Задачи:**
1. Создать модель Widget
2. Реализовать CRUD API
3. Добавить reorder endpoint
4. Реализовать загрузку виджетов модулей

### Шаг 6.2: Frontend Dashboard

**Компоненты:**
```typescript
// components/dashboard/
├── DashboardGrid.tsx      // Сетка виджетов
├── WidgetContainer.tsx    // Контейнер виджета
├── WidgetLoader.tsx       // Загрузчик виджета модуля
└── AddWidgetModal.tsx     // Модальное окно добавления
```

**Задачи:**
1. Создать DashboardGrid с drag & drop
2. Реализовать WidgetContainer
3. Создать WidgetLoader (динамическая загрузка)
4. Реализовать AddWidgetModal
5. Добавить сохранение позиций
6. Реализовать resize виджетов (опционально)

**Проверка:**
- ✅ Виджеты отображаются на Dashboard
- ✅ Можно добавить/удалить виджет
- ✅ Можно перетаскивать виджеты
- ✅ Позиции сохраняются

---

## Этап 7: Уведомления и события (Неделя 15-16)

### Шаг 7.1: Система уведомлений

**Backend:**
```python
# models/notification.py
class Notification(Base):
    id: str
    user_id: str
    module_id: str
    title: str
    message: str
    type: str  # info, success, warning, error
    priority: str  # low, normal, high, critical
    read: bool
    action_url: str
    created_at: datetime

# api/notifications.py
GET    /notifications        # Список уведомлений
POST   /notifications        # Создать (для модулей)
PUT    /notifications/{id}/read  # Отметить прочитанным
DELETE /notifications/{id}   # Удалить
```

**WebSocket для real-time:**
```python
# api/websocket.py
@app.websocket("/ws")
async def websocket_endpoint(websocket: WebSocket):
    await websocket.accept()
    user_id = await authenticate_websocket(websocket)
    
    # Подписать на уведомления
    await subscribe_to_notifications(user_id, websocket)
```

**Задачи:**
1. Создать модель Notification
2. Реализовать API уведомлений
3. Добавить WebSocket endpoint
4. Реализовать real-time отправку
5. Добавить в SDK метод notify()

### Шаг 7.2: Frontend уведомлений

**Компоненты:**
```typescript
// components/notifications/
├── NotificationBell.tsx   // Иконка с счётчиком
├── NotificationList.tsx   // Список уведомлений
├── NotificationItem.tsx   // Элемент уведомления
└── NotificationToast.tsx  // Toast уведомление
```

**Задачи:**
1. Создать NotificationBell в Header
2. Реализовать NotificationList (dropdown)
3. Добавить WebSocket подключение
4. Реализовать Toast уведомления
5. Добавить звук (опционально)
6. Реализовать отметку прочитанным

**Проверка:**
- ✅ Уведомления приходят в real-time
- ✅ Показываются в Header
- ✅ Toast появляется при новом уведомлении
- ✅ Можно отметить прочитанным

---

## Этап 8: Первые модули (Неделя 17-20)

### Создать 3-5 базовых модулей:

**1. Sleep Tracker**
- База данных: sleep_records
- API: record, stats, trends
- UI: график сна, форма записи
- ИИ: анализ паттернов, рекомендации

**2. Habit Tracker**
- База данных: habits, logs
- API: create_habit, log, stats
- UI: список привычек, календарь
- ИИ: мотивация, напоминания

**3. Finance Manager**
- База данных: transactions, categories
- API: add_transaction, balance, report
- UI: таблица трат, графики
- ИИ: анализ трат, бюджет

**4. Study Assistant**
- База данных: subjects, tasks, notes
- API: add_task, schedule, notes
- UI: расписание, задачи
- ИИ: помощь с учёбой

**5. Fitness Tracker**
- База данных: workouts, exercises
- API: log_workout, stats
- UI: журнал тренировок
- ИИ: планирование тренировок

**Задачи для каждого модуля:**
1. Создать манифест (module.yaml)
2. Реализовать модели БД
3. Создать API endpoints
4. Реализовать UI компоненты
5. Добавить ИИ функции
6. Написать документацию
7. Создать примеры использования
8. Протестировать интеграцию

---

## Этап 9: Полировка и оптимизация (Неделя 21-22)

### Задачи:

**Backend:**
- [ ] Оптимизация запросов к БД
- [ ] Кэширование (Redis)
- [ ] Rate limiting
- [ ] Логирование и мониторинг
- [ ] Обработка ошибок
- [ ] Тесты (pytest)

**Frontend:**
- [ ] Оптимизация производительности
- [ ] Lazy loading компонентов
- [ ] Оптимизация изображений
- [ ] SEO оптимизация
- [ ] Accessibility (a11y)
- [ ] Тесты (Jest, Playwright)

**ИИ:**
- [ ] Оптимизация промптов
- [ ] Кэширование ответов
- [ ] Обработка ошибок OpenAI
- [ ] Fallback на другие модели
- [ ] Мониторинг токенов

**Документация:**
- [ ] API документация (Swagger)
- [ ] SDK документация
- [ ] Руководство пользователя
- [ ] Руководство разработчика
- [ ] Примеры модулей

---

## Этап 10: Деплой и запуск (Неделя 23-24)

### Подготовка к продакшену:

**Инфраструктура:**
- [ ] Настроить Docker
- [ ] Настроить CI/CD (GitHub Actions)
- [ ] Выбрать хостинг (AWS/GCP/DigitalOcean)
- [ ] Настроить PostgreSQL
- [ ] Настроить Redis
- [ ] Настроить Nginx

**Безопасность:**
- [ ] HTTPS сертификаты
- [ ] Защита от CSRF
- [ ] Rate limiting
- [ ] Валидация данных
- [ ] Санитизация SQL
- [ ] Secrets management

**Мониторинг:**
- [ ] Sentry для ошибок
- [ ] Prometheus + Grafana для метрик
- [ ] Логирование (ELK stack)
- [ ] Uptime monitoring

**Запуск:**
- [ ] Beta тестирование
- [ ] Сбор обратной связи
- [ ] Исправление критических багов
- [ ] Публичный запуск
- [ ] Маркетинг и продвижение

---

## 📊 Итоговый чек-лист

### MVP (Минимально жизнеспособный продукт):
- [ ] Регистрация и вход
- [ ] Календарь с событиями
- [ ] Чат с ИИ (базовый)
- [ ] Создание модулей
- [ ] Маркетплейс модулей
- [ ] 3-5 базовых модулей
- [ ] Dashboard с виджетами
- [ ] Уведомления

### Дополнительно (после MVP):
- [ ] Синхронизация с Google Calendar
- [ ] Голосовой ввод в чат
- [ ] Мобильное приложение
- [ ] Биллинг и подписки
- [ ] Аналитика для разработчиков
- [ ] Marketplace revenue sharing
- [ ] Интеграции (Telegram, Discord)
- [ ] API для сторонних приложений

---

## 🎯 Приоритеты

**Высокий приоритет (Must have):**
1. Аутентификация
2. Календарь
3. Чат с ИИ
4. Система модулей
5. SDK для разработчиков

**Средний приоритет (Should have):**
6. Dashboard с виджетами
7. Уведомления
8. Базовые модули (3-5 штук)
9. Маркетплейс

**Низкий приоритет (Nice to have):**
10. Синхронизация с внешними сервисами
11. Голосовой ввод
12. Мобильное приложение
13. Биллинг

---

## ⏱️ Временные оценки

- **MVP:** 12-16 недель (3-4 месяца)
- **Полная версия:** 20-24 недели (5-6 месяцев)
- **С дополнительными фичами:** 30+ недель (7+ месяцев)

**Команда:**
- 1 Backend разработчик
- 1 Frontend разработчик
- 1 AI/ML инженер
- 1 DevOps (part-time)
- 1 Designer (part-time)

**Или:**
- 1 Full-stack разработчик (увеличить сроки в 1.5-2 раза)
