# План разработки Eidos с чекпоинтами

## 📋 Как использовать этот план

После завершения каждого чекпоинта:
1. ✅ Отметьте все задачи как выполненные
2. 🧪 Запустите тесты
3. 💾 Сделайте commit с тегом `checkpoint-X`
4. 📸 Сделайте backup базы данных
5. ➡️ Переходите к следующему чекпоинту

---

## CHECKPOINT 0: Подготовка окружения

### Задачи:
- [ ] Установить Python 3.11+
- [ ] Установить Node.js 18+
- [ ] Установить PostgreSQL (или использовать SQLite для dev)
- [ ] Установить Git
- [ ] Создать репозиторий на GitHub
- [ ] Настроить виртуальное окружение Python
- [ ] Установить зависимости backend
- [ ] Установить зависимости frontend

### Команды:
```bash
# Backend
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install fastapi sqlalchemy alembic python-jose bcrypt python-multipart

# Frontend
cd frontend
npm install next react react-dom
npm install -D tailwindcss postcss autoprefixer
npm install axios zustand lucide-react
```

### Проверка:
```bash
python --version  # 3.11+
node --version    # 18+
```

**Время:** 1 час

---

## CHECKPOINT 1: Backend - Базовая структура

### Задачи:
- [ ] Создать структуру папок backend
- [ ] Настроить FastAPI приложение
- [ ] Настроить SQLAlchemy
- [ ] Создать базовый конфиг
- [ ] Настроить CORS
- [ ] Создать health check endpoint

### Файлы для создания:
```
backend/
├── main.py
├── core/
│   ├── __init__.py
│   ├── config.py
│   └── database.py
├── models/
│   └── __init__.py
└── api/
    └── __init__.py
```

### Код:

**main.py:**
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from core.database import init_db

app = FastAPI(title="Eidos API")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.on_event("startup")
async def startup():
    await init_db()

@app.get("/health")
async def health():
    return {"status": "healthy"}

if __name__ == "__main__":
    import uvicorn
    uvicorn.run("main:app", host="0.0.0.0", port=8001, reload=True)
```

### Проверка:
```bash
cd backend
python main.py
# Открыть http://localhost:8001/health
# Должно вернуть: {"status": "healthy"}
```

**Время:** 2 часа

---

## CHECKPOINT 2: Backend - Аутентификация

### Задачи:
- [ ] Создать модель User
- [ ] Реализовать хеширование паролей (bcrypt)
- [ ] Реализовать JWT токены
- [ ] Создать endpoints: register, login, me
- [ ] Добавить middleware для проверки токена

### Файлы:
```
backend/
├── models/
│   └── user.py
├── core/
│   └── security.py
└── api/
    └── auth.py
```

### Код:

**models/user.py:**
```python
from sqlalchemy import Column, String, DateTime, Enum
from datetime import datetime
import uuid
import enum
from core.database import Base

class UserRole(str, enum.Enum):
    USER = "user"
    ADMIN = "admin"

class User(Base):
    __tablename__ = "users"
    
    id = Column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
    email = Column(String, unique=True, nullable=False, index=True)
    password_hash = Column(String, nullable=False)
    name = Column(String, nullable=False)
    role = Column(Enum(UserRole), default=UserRole.USER)
    created_at = Column(DateTime, default=datetime.utcnow)
```

**api/auth.py:**
```python
from fastapi import APIRouter, Depends, HTTPException
from pydantic import BaseModel
from core.security import hash_password, verify_password, create_token

router = APIRouter()

class RegisterRequest(BaseModel):
    name: str
    email: str
    password: str

class LoginRequest(BaseModel):
    email: str
    password: str

@router.post("/register")
async def register(request: RegisterRequest):
    # Проверить email
    # Создать пользователя
    # Вернуть токен
    pass

@router.post("/login")
async def login(request: LoginRequest):
    # Проверить credentials
    # Вернуть токен
    pass

@router.get("/me")
async def get_me(current_user = Depends(get_current_user)):
    return current_user
```

### Проверка:
```bash
# Регистрация
curl -X POST http://localhost:8001/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"name":"Test","email":"test@test.com","password":"123456"}'

# Вход
curl -X POST http://localhost:8001/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"123456"}'
```

**Время:** 4 часа

---

## CHECKPOINT 3: Frontend - Базовая структура

### Задачи:
- [ ] Создать Next.js приложение
- [ ] Настроить TailwindCSS
- [ ] Создать layout структуру
- [ ] Настроить Axios
- [ ] Создать auth store (Zustand)

### Файлы:
```
frontend/src/
├── app/
│   ├── layout.tsx
│   └── page.tsx
├── lib/
│   └── api.ts
└── store/
    └── authStore.ts
```

### Код:

**lib/api.ts:**
```typescript
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://localhost:8001/api/v1',
})

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

export default api
```

**store/authStore.ts:**
```typescript
import { create } from 'zustand'

interface AuthState {
  token: string | null
  user: any | null
  setAuth: (token: string, user: any) => void
  logout: () => void
}

export const useAuthStore = create<AuthState>((set) => ({
  token: null,
  user: null,
  setAuth: (token, user) => set({ token, user }),
  logout: () => set({ token: null, user: null }),
}))
```

### Проверка:
```bash
cd frontend
npm run dev
# Открыть http://localhost:3000
```

**Время:** 3 часа

---

## CHECKPOINT 4: Frontend - Аутентификация

### Задачи:
- [ ] Создать страницу регистрации
- [ ] Создать страницу входа
- [ ] Реализовать защищённые роуты
- [ ] Добавить редирект после входа

### Файлы:
```
frontend/src/app/
├── (auth)/
│   ├── login/page.tsx
│   └── register/page.tsx
└── (dashboard)/
    ├── layout.tsx
    └── page.tsx
```

### Проверка:
- [ ] Можно зарегистрироваться
- [ ] Можно войти
- [ ] Токен сохраняется в localStorage
- [ ] Редирект на /dashboard после входа
- [ ] Защищённые страницы требуют авторизации

**Время:** 4 часа

---

## CHECKPOINT 5: Backend - Календарь (модели и API)

### Задачи:
- [ ] Создать модель CalendarEvent
- [ ] Реализовать CRUD endpoints
- [ ] Добавить фильтрацию по датам
- [ ] Реализовать повторяющиеся события

### Файлы:
```
backend/
├── models/
│   └── calendar.py
└── api/
    └── calendar.py
```

### Endpoints:
```
POST   /calendar/events
GET    /calendar/events
GET    /calendar/events/{id}
PUT    /calendar/events/{id}
DELETE /calendar/events/{id}
GET    /calendar/events/range?start=...&end=...
```

### Проверка:
```bash
# Создать событие
curl -X POST http://localhost:8001/api/v1/calendar/events \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Встреча",
    "start_time": "2025-11-15T10:00:00",
    "end_time": "2025-11-15T11:00:00"
  }'

# Получить события
curl http://localhost:8001/api/v1/calendar/events \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Время:** 4 часа

---

## CHECKPOINT 6: Frontend - Календарь (UI)

### Задачи:
- [ ] Создать компонент CalendarView
- [ ] Реализовать MonthView (сетка)
- [ ] Создать EventModal (создание/редактирование)
- [ ] Добавить переключение видов
- [ ] Реализовать создание событий

### Файлы:
```
frontend/src/
├── app/(dashboard)/calendar/page.tsx
└── components/calendar/
    ├── CalendarView.tsx
    ├── MonthView.tsx
    ├── EventCard.tsx
    └── EventModal.tsx
```

### Проверка:
- [ ] Календарь отображается
- [ ] Можно создать событие
- [ ] События отображаются в календаре
- [ ] Можно редактировать событие
- [ ] Можно удалить событие

**Время:** 6 часов

---

## CHECKPOINT 7: Backend - Чат (модели и OpenAI)

### Задачи:
- [ ] Создать модели Conversation и Message
- [ ] Настроить OpenAI API
- [ ] Создать AIEngine класс
- [ ] Реализовать chat endpoint
- [ ] Добавить function calling для календаря

### Файлы:
```
backend/
├── models/
│   └── chat.py
├── core/
│   └── ai.py
└── api/
    ├── chat.py
    └── ai_functions.py
```

### Код:

**core/ai.py:**
```python
from openai import AsyncOpenAI

class AIEngine:
    def __init__(self):
        self.client = AsyncOpenAI(api_key=settings.OPENAI_API_KEY)
    
    async def chat(self, messages: list, functions: list = None):
        response = await self.client.chat.completions.create(
            model="gpt-4-turbo-preview",
            messages=messages,
            functions=functions,
            function_call="auto"
        )
        return response
```

### Проверка:
```bash
# Отправить сообщение
curl -X POST http://localhost:8001/api/v1/chat/message \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "Привет!"}'
```

**Время:** 6 часов

---

## CHECKPOINT 8: Frontend - Чат (UI)

### Задачи:
- [ ] Создать интерфейс чата
- [ ] Реализовать отправку сообщений
- [ ] Добавить отображение истории
- [ ] Реализовать автоскролл
- [ ] Добавить индикатор печати

### Файлы:
```
frontend/src/
├── app/(dashboard)/chat/page.tsx
└── components/chat/
    ├── ChatInterface.tsx
    ├── MessageList.tsx
    ├── MessageBubble.tsx
    └── ChatInput.tsx
```

### Проверка:
- [ ] Можно отправить сообщение
- [ ] ИИ отвечает
- [ ] История сохраняется
- [ ] Автоскролл работает
- [ ] ИИ может создать событие в календаре

**Время:** 5 часов

---

## CHECKPOINT 9: Backend - Система модулей

### Задачи:
- [ ] Создать модели Module, UserModule
- [ ] Реализовать CRUD для модулей
- [ ] Добавить генерацию API ключа
- [ ] Реализовать установку/удаление модулей
- [ ] Создать систему разрешений

### Файлы:
```
backend/
├── models/
│   └── module.py
└── api/
    └── modules.py
```

### Endpoints:
```
GET    /modules              # Список модулей
POST   /modules              # Создать модуль
GET    /modules/{id}         # Получить модуль
PUT    /modules/{id}         # Обновить
DELETE /modules/{id}         # Удалить
POST   /modules/{id}/install # Установить
```

### Проверка:
```bash
# Создать модуль
curl -X POST http://localhost:8001/api/v1/modules \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Sleep Tracker",
    "description": "Отслеживание сна",
    "manifest": {}
  }'
```

**Время:** 6 часов

---

## CHECKPOINT 10: Python SDK

### Задачи:
- [ ] Создать структуру SDK
- [ ] Реализовать EidosModule базовый класс
- [ ] Создать EidosClient
- [ ] Добавить декораторы
- [ ] Написать документацию
- [ ] Создать пример модуля

### Файлы:
```
sdk/python/
├── eidos_sdk/
│   ├── __init__.py
│   ├── module.py
│   ├── client.py
│   └── decorators.py
├── examples/
│   └── sleep_tracker.py
├── setup.py
└── README.md
```

### Проверка:
```bash
cd sdk/python
pip install -e .

# Запустить пример
python examples/sleep_tracker.py
```

**Время:** 8 часов

---

## CHECKPOINT 11: Frontend - Маркетплейс модулей

### Задачи:
- [ ] Создать страницу списка модулей
- [ ] Реализовать фильтры
- [ ] Создать страницу модуля
- [ ] Добавить установку модуля
- [ ] Создать страницу "Мои модули"

### Файлы:
```
frontend/src/app/(dashboard)/modules/
├── page.tsx
├── [id]/page.tsx
├── new/page.tsx
└── my/page.tsx
```

### Проверка:
- [ ] Список модулей отображается
- [ ] Фильтры работают
- [ ] Можно открыть страницу модуля
- [ ] Можно установить модуль
- [ ] "Мои модули" показывает созданные модули

**Время:** 6 часов

---

## CHECKPOINT 12: Интеграция модулей с ИИ

### Задачи:
- [ ] Создать AIContextBuilder
- [ ] Реализовать сбор контекста модулей
- [ ] Добавить динамический system prompt
- [ ] Реализовать маршрутизацию функций
- [ ] Добавить ModuleAPI

### Файлы:
```
backend/
├── core/
│   └── ai_context.py
└── api/
    └── module_api.py
```

### Проверка:
- [ ] ИИ видит установленные модули
- [ ] ИИ может вызывать функции модулей
- [ ] Модули могут вызывать друг друга
- [ ] Контекст обновляется при установке модуля

**Время:** 6 часов

---

## CHECKPOINT 13: Dashboard с виджетами

### Задачи:
- [ ] Создать модель Widget
- [ ] Реализовать API виджетов
- [ ] Создать DashboardGrid
- [ ] Добавить drag & drop
- [ ] Реализовать загрузку виджетов модулей

### Файлы:
```
backend/
├── models/
│   └── widget.py
└── api/
    └── widgets.py

frontend/src/
├── app/(dashboard)/page.tsx
└── components/dashboard/
    ├── DashboardGrid.tsx
    └── WidgetContainer.tsx
```

### Проверка:
- [ ] Dashboard отображается
- [ ] Виджеты загружаются
- [ ] Можно перетаскивать виджеты
- [ ] Позиции сохраняются

**Время:** 6 часов

---

## CHECKPOINT 14: Уведомления

### Задачи:
- [ ] Создать модель Notification
- [ ] Реализовать API уведомлений
- [ ] Добавить WebSocket
- [ ] Создать NotificationBell
- [ ] Реализовать Toast уведомления

### Файлы:
```
backend/
├── models/
│   └── notification.py
└── api/
    ├── notifications.py
    └── websocket.py

frontend/src/components/
├── NotificationBell.tsx
└── NotificationToast.tsx
```

### Проверка:
- [ ] Уведомления приходят в real-time
- [ ] Показываются в Header
- [ ] Toast появляется
- [ ] Можно отметить прочитанным

**Время:** 5 часов

---

## CHECKPOINT 15: Первый модуль - Sleep Tracker

### Задачи:
- [ ] Создать манифест модуля
- [ ] Реализовать модели БД
- [ ] Создать API endpoints
- [ ] Реализовать UI компоненты
- [ ] Добавить ИИ функции
- [ ] Протестировать

### Структура:
```
modules/sleep-tracker/
├── module.yaml
├── api/routes.py
├── models.py
├── ui/SleepPage.tsx
└── ai/functions.py
```

### Проверка:
- [ ] Модуль устанавливается
- [ ] Можно записать сон
- [ ] Статистика отображается
- [ ] ИИ может работать с модулем
- [ ] Виджет на Dashboard работает

**Время:** 8 часов

---

## CHECKPOINT 16: Второй модуль - Habit Tracker

### Задачи:
- [ ] Создать манифест
- [ ] Реализовать БД
- [ ] Создать API
- [ ] Реализовать UI
- [ ] Добавить ИИ функции

### Проверка:
- [ ] Можно создать привычку
- [ ] Можно отметить выполнение
- [ ] Streaks считаются
- [ ] ИИ может мотивировать

**Время:** 8 часов

---

## CHECKPOINT 17: Третий модуль - Finance Manager

### Задачи:
- [ ] Создать манифест
- [ ] Реализовать БД
- [ ] Создать API
- [ ] Реализовать UI
- [ ] Добавить ИИ функции

### Проверка:
- [ ] Можно добавить транзакцию
- [ ] Баланс считается
- [ ] Статистика отображается
- [ ] ИИ анализирует траты

**Время:** 8 часов

---

## CHECKPOINT 18: Тестирование и баги

### Задачи:
- [ ] Написать unit тесты (backend)
- [ ] Написать integration тесты
- [ ] Написать E2E тесты (frontend)
- [ ] Исправить найденные баги
- [ ] Оптимизировать производительность

### Проверка:
- [ ] Все тесты проходят
- [ ] Нет критических багов
- [ ] Производительность приемлемая

**Время:** 10 часов

---

## CHECKPOINT 19: Документация

### Задачи:
- [ ] API документация (Swagger)
- [ ] SDK документация
- [ ] Руководство пользователя
- [ ] Руководство разработчика
- [ ] Примеры модулей

### Файлы:
```
docs/
├── API.md
├── SDK.md
├── USER_GUIDE.md
├── DEVELOPER_GUIDE.md
└── examples/
```

**Время:** 6 часов

---

## CHECKPOINT 20: Деплой

### Задачи:
- [ ] Настроить Docker
- [ ] Настроить CI/CD
- [ ] Выбрать хостинг
- [ ] Настроить PostgreSQL
- [ ] Настроить HTTPS
- [ ] Задеплоить

### Проверка:
- [ ] Приложение доступно по URL
- [ ] HTTPS работает
- [ ] БД подключена
- [ ] CI/CD работает

**Время:** 8 часов

---

## 📊 Итоговая статистика

**Всего чекпоинтов:** 20

**Общее время:**
- Минимум: 120 часов (3 недели full-time)
- Реально: 160-200 часов (4-5 недель full-time)

**По этапам:**
- Инфраструктура (CP 0-4): 14 часов
- Календарь (CP 5-6): 10 часов
- Чат с ИИ (CP 7-8): 11 часов
- Система модулей (CP 9-12): 26 часов
- Dashboard (CP 13-14): 11 часов
- Модули (CP 15-17): 24 часов
- Финализация (CP 18-20): 24 часов

**MVP (минимум):** Чекпоинты 0-14 (76 часов)
**Полная версия:** Все чекпоинты (120+ часов)

---

## 🎯 Рекомендации

1. **Не пропускайте чекпоинты** - каждый строится на предыдущем
2. **Делайте commits** после каждого чекпоинта
3. **Тестируйте** перед переходом к следующему
4. **Делайте backup** базы данных
5. **Документируйте** изменения

**Удачи в разработке! 🚀**
