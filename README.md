# 🤖 EIDOS - Персональный ИИ-ассистент

> Умный ассистент для управления жизнью через MAKS мессенджер и веб-интерфейс

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка и запуск](#-установка-и-запуск)
  - [Вариант 1: Docker (рекомендуется)](#вариант-1-docker-рекомендуется)
  - [Вариант 2: Локальный запуск](#вариант-2-локальный-запуск)
  - [Вариант 3: Запуск через командную строку](#вариант-3-запуск-через-командную-строку)
- [Настройка](#-настройка)
- [Использование](#-использование)
- [Архитектура](#-архитектура)
- [API Документация](#-api-документация)
- [Технологии](#-технологии)
- [Устранение неполадок](#-устранение-неполадок)
- [Поддержка](#-поддержка)

---

## ✨ Возможности

### 📱 MAKS Бот
- **Естественное общение** - просто пишите как обычно
- **Умное понимание** - AI распознает намерения автоматически
- **Быстрые команды** - `/start`, `/help`, `/stats`

### 📊 Модули

#### 🌙 Sleep Tracker
- Отслеживание качества сна
- Анализ паттернов
- Рекомендации по улучшению

#### 💪 Habit Tracker
- Создание привычек
- Ежедневное отслеживание
- Статистика выполнения

#### 💰 Finance Manager
- Учет доходов и расходов
- Категоризация транзакций
- Финансовая аналитика

#### 📅 Calendar
- Создание событий через бота
- Напоминания
- Синхронизация с веб-версией

### 🌐 Веб-интерфейс
- Красивый дашборд с современным UI
- Детальная статистика и аналитика
- Управление всеми данными
- Безопасный вход через MAKS

---

## 🔧 Требования

### Системные требования

**Для Docker:**
- Docker Engine 20.10+
- Docker Compose 1.29+
- 2 GB свободной оперативной памяти
- 5 GB свободного места на диске

**Для локального запуска:**
- Python 3.10 или выше
- Node.js 18.0+ и npm 9.0+
- SQLite 3.35+
- 1 GB свободной оперативной памяти

### API Ключи

Перед запуском необходимо получить:
1. **DeepSeek API ключ** - для работы AI (регистрация на [deepseek.com](https://deepseek.com))(используйте: sk-84774fc208684f8eb3742d50b7671b91)
2. **MAKS Bot токен** - для работы бота (создание бота в MAKS)

---

## 🚀 Установка и запуск

### Вариант 1: Docker (рекомендуется)

Docker - самый простой способ запуска всего проекта одной командой.

#### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/your-username/EIDOS.git
cd EIDOS/MAX
```

#### Шаг 2: Настройка переменных окружения

Создайте файл `.env` на основе примера:

```bash
# Windows (PowerShell)
Copy-Item .env.example .env

# Linux/Mac
cp .env.example .env
```

Откройте `.env` и заполните необходимые значения:

```env
# Backend
SECRET_KEY=your-secret-key-change-in-production-use-random-64-chars
DATABASE_URL=sqlite+aiosqlite:///./eidos.db
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Bot
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxx
BOT_TOKEN=your-maks-bot-token-from-maks

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8001
```

**Важно:** 
- Замените `SECRET_KEY` на случайную строку длиной 64 символа
- Вставьте ваш реальный `DEEPSEEK_API_KEY`
- Вставьте ваш `BOT_TOKEN` из MAKS

#### Шаг 3: Запуск контейнеров

```bash
# Запуск всех сервисов в фоновом режиме
docker-compose up -d

# Просмотр логов (опционально)
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f bot
```

#### Шаг 4: Проверка запуска

После запуска проверьте доступность сервисов:

- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8001
- **API Docs:** http://localhost:8001/docs
- **Health Check:** http://localhost:8001/health

#### Управление контейнерами

```bash
# Остановка всех сервисов
docker-compose down

# Остановка с удалением volumes (все данные будут удалены!)
docker-compose down -v

# Перезапуск всех сервисов
docker-compose restart

# Перезапуск конкретного сервиса
docker-compose restart backend

# Просмотр статуса
docker-compose ps

# Обновление после изменений в коде
docker-compose up -d --build
```

---

### Вариант 2: Локальный запуск

Для разработки или если Docker недоступен, можно запустить каждый компонент отдельно.

#### Предварительная подготовка

Создайте файл `.env` (см. Вариант 1, Шаг 2) в корневой директории MAX.

#### Шаг 1: Запуск Backend

```bash
# Переход в директорию backend
cd Web/backend

# Создание виртуального окружения (рекомендуется)
python -m venv venv

# Активация виртуального окружения
# Windows (PowerShell):
venv\Scripts\Activate.ps1
# Windows (CMD):
venv\Scripts\activate.bat
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера (порт 8001)
python main.py
```

Backend будет доступен на http://localhost:8001

#### Шаг 2: Запуск Frontend

Откройте **новый терминал** и выполните:

```bash
# Переход в директорию frontend
cd Web/frontend

# Установка зависимостей
npm install

# Запуск dev-сервера (порт 3000)
npm run dev

# Альтернативно: production build
npm run build
npm start
```

Frontend будет доступен на http://localhost:3000

#### Шаг 3: Запуск MAKS Bot

Откройте **третий терминал** и выполните:

```bash
# Переход в директорию chatbot
cd chatbot

# Создание виртуального окружения (если еще не создано)
python -m venv venv

# Активация виртуального окружения
# Windows (PowerShell):
venv\Scripts\Activate.ps1
# Windows (CMD):
venv\Scripts\activate.bat
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Запуск бота
python bot_ai.py
```

Бот начнет работу и будет отвечать в MAKS мессенджере.

---

### Вариант 3: Запуск через командную строку

#### Быстрый запуск всего проекта (Windows PowerShell)

Создайте файл `start.ps1`:

```powershell
# start.ps1 - Скрипт запуска EIDOS

Write-Host "🚀 Запуск EIDOS..." -ForegroundColor Green

# Проверка .env файла
if (-not (Test-Path ".env")) {
    Write-Host "❌ Файл .env не найден! Создайте его на основе .env.example" -ForegroundColor Red
    exit 1
}

# Запуск Backend
Write-Host "📦 Запуск Backend..." -ForegroundColor Yellow
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd Web\backend; python -m venv venv; .\venv\Scripts\Activate.ps1; pip install -r requirements.txt; python main.py"

# Ожидание запуска Backend
Start-Sleep -Seconds 5

# Запуск Frontend
Write-Host "🌐 Запуск Frontend..." -ForegroundColor Yellow
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd Web\frontend; npm install; npm run dev"

# Запуск Bot
Write-Host "🤖 Запуск MAKS Bot..." -ForegroundColor Yellow
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd chatbot; python -m venv venv; .\venv\Scripts\Activate.ps1; pip install -r requirements.txt; python bot_ai.py"

Write-Host "✅ Все сервисы запущены!" -ForegroundColor Green
Write-Host "Frontend: http://localhost:3000" -ForegroundColor Cyan
Write-Host "Backend: http://localhost:8001" -ForegroundColor Cyan
Write-Host "API Docs: http://localhost:8001/docs" -ForegroundColor Cyan
```

Запуск:

```powershell
# Разрешить выполнение скриптов (один раз)
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned

# Запуск
.\start.ps1
```

#### Быстрый запуск всего проекта (Linux/Mac)

Создайте файл `start.sh`:

```bash
#!/bin/bash
# start.sh - Скрипт запуска EIDOS

echo "🚀 Запуск EIDOS..."

# Проверка .env файла
if [ ! -f .env ]; then
    echo "❌ Файл .env не найден! Создайте его на основе .env.example"
    exit 1
fi

# Запуск Backend
echo "📦 Запуск Backend..."
gnome-terminal -- bash -c "cd Web/backend && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && python main.py; exec bash"

# Ожидание запуска Backend
sleep 5

# Запуск Frontend
echo "🌐 Запуск Frontend..."
gnome-terminal -- bash -c "cd Web/frontend && npm install && npm run dev; exec bash"

# Запуск Bot
echo "🤖 Запуск MAKS Bot..."
gnome-terminal -- bash -c "cd chatbot && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && python bot_ai.py; exec bash"

echo "✅ Все сервисы запущены!"
echo "Frontend: http://localhost:3000"
echo "Backend: http://localhost:8001"
echo "API Docs: http://localhost:8001/docs"
```

Запуск:

```bash
# Сделать исполняемым
chmod +x start.sh

# Запуск
./start.sh
```

#### Остановка сервисов

```bash
# Найти и завершить процессы Python/Node
# Windows (PowerShell):
Get-Process python,node | Stop-Process -Force

# Linux/Mac:
pkill -f "python|node"
```

---

## ⚙️ Настройка

### Переменные окружения

Полный список переменных в `.env`:

| Переменная | Описание | Значение по умолчанию |
|-----------|----------|----------------------|
| `SECRET_KEY` | Секретный ключ для JWT токенов (64 символа) | - |
| `DATABASE_URL` | URL подключения к БД | `sqlite+aiosqlite:///./eidos.db` |
| `ALGORITHM` | Алгоритм шифрования JWT | `HS256` |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Время жизни токена в минутах | `10080` (7 дней) |
| `DEEPSEEK_API_KEY` | API ключ DeepSeek AI | - |
| `BOT_TOKEN` | Токен MAKS бота | - |
| `NEXT_PUBLIC_API_URL` | URL Backend API для Frontend | `http://localhost:8001` |

### Генерация SECRET_KEY

```python
# Python
import secrets
print(secrets.token_urlsafe(64))
```

```bash
# Linux/Mac
openssl rand -base64 64
```

```powershell
# Windows PowerShell
[Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(64))
```

---

## 📖 Использование

### 1. Первый запуск

После запуска всех сервисов:

1. Откройте MAKS мессенджер
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Бот вышлет вам логин и пароль для веб-интерфейса

### 2. Вход в веб-интерфейс

1. Откройте http://localhost:3000/maks-login
2. Введите логин и пароль, полученные от бота
3. Вы попадете на главный дашборд

### 3. Примеры команд для бота

**Сон:**
```
Я спал 8 часов, качество 9/10
Вчера спал плохо, 5 часов
Покажи статистику сна за неделю
```

**Привычки:**
```
Создай привычку "Медитация" каждый день
Я выполнил медитацию
Покажи мои привычки
Удали привычку "Медитация"
```

**Финансы:**
```
Потратил 500 рублей на продукты
Получил зарплату 50000
Покажи расходы за месяц
Категория еда: 500 рублей
```

**Календарь:**
```
Добавь встречу завтра в 15:00 - Встреча с клиентом
Создай напоминание на вторник 19:00 - Спортзал
Покажи события на неделю
```

### 4. Работа через API

Примеры запросов к API:

```bash
# Получение токена
curl -X POST "http://localhost:8001/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"username": "user123", "password": "your-password"}'

# Создание записи о сне
curl -X POST "http://localhost:8001/api/sleep" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"date": "2024-01-15", "hours": 8, "quality": 9}'

# Получение статистики
curl -X GET "http://localhost:8001/api/stats" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 🏗️ Архитектура

```
EIDOS/
├── MAX/                          # Основной проект
│   ├── chatbot/                  # MAKS бот
│   │   ├── bot_ai.py            # Главный файл бота
│   │   ├── integrations/        # Интеграции с модулями
│   │   ├── requirements.txt     # Python зависимости
│   │   └── Dockerfile           # Docker образ бота
│   │
│   ├── ai_core/                 # AI движок
│   │   ├── engine.py            # UnifiedAIEngine
│   │   ├── tools.py             # AI инструменты
│   │   └── context.py           # Контекст управление
│   │
│   ├── Web/                     # Веб-приложение
│   │   ├── backend/             # FastAPI Backend
│   │   │   ├── api/            # API endpoints
│   │   │   │   ├── auth.py     # Аутентификация
│   │   │   │   ├── sleep.py    # Sleep Tracker API
│   │   │   │   ├── habits.py   # Habit Tracker API
│   │   │   │   ├── finance.py  # Finance API
│   │   │   │   └── calendar.py # Calendar API
│   │   │   ├── core/           # Ядро приложения
│   │   │   │   ├── config.py   # Настройки
│   │   │   │   ├── security.py # Безопасность
│   │   │   │   └── database.py # БД подключение
│   │   │   ├── models/         # SQLAlchemy модели
│   │   │   ├── main.py         # Точка входа
│   │   │   ├── requirements.txt
│   │   │   └── Dockerfile
│   │   │
│   │   └── frontend/            # Next.js Frontend
│   │       ├── src/
│   │       │   ├── app/        # App Router страницы
│   │       │   ├── components/ # React компоненты
│   │       │   └── lib/        # Утилиты
│   │       ├── package.json
│   │       └── Dockerfile
│   │
│   ├── docker-compose.yml       # Docker Compose конфиг
│   ├── .env.example             # Пример переменных окружения
│   ├── requirements.txt         # Общие Python зависимости
│   └── README.md                # Эта документация
```

### Потоки данных

```
┌─────────────┐         ┌──────────────┐         ┌──────────────┐
│   MAKS Bot  │ ◄─────► │  AI Engine   │ ◄─────► │   Backend    │
│  (chatbot)  │         │  (ai_core)   │         │   (FastAPI)  │
└─────────────┘         └──────────────┘         └──────────────┘
                                                         ▲
                                                         │
                                                         ▼
                                                  ┌──────────────┐
                                                  │   Database   │
                                                  │   (SQLite)   │
                                                  └──────────────┘
                                                         ▲
                                                         │
                                                         ▼
                                                  ┌──────────────┐
                                                  │   Frontend   │
                                                  │  (Next.js)   │
                                                  └──────────────┘
```

---

## 📚 API Документация

### Swagger UI

После запуска Backend API, документация доступна по адресу:
- **Swagger UI:** http://localhost:8001/docs
- **ReDoc:** http://localhost:8001/redoc

### Основные endpoints

#### Аутентификация
- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Получение текущего пользователя

#### Sleep Tracker
- `GET /api/sleep` - Получить записи о сне
- `POST /api/sleep` - Создать запись
- `PUT /api/sleep/{id}` - Обновить запись
- `DELETE /api/sleep/{id}` - Удалить запись

#### Habit Tracker
- `GET /api/habits` - Получить привычки
- `POST /api/habits` - Создать привычку
- `POST /api/habits/{id}/complete` - Отметить выполнение
- `DELETE /api/habits/{id}` - Удалить привычку

#### Finance Manager
- `GET /api/finance/transactions` - Получить транзакции
- `POST /api/finance/transactions` - Создать транзакцию
- `GET /api/finance/stats` - Получить статистику

#### Calendar
- `GET /api/calendar/events` - Получить события
- `POST /api/calendar/events` - Создать событие
- `DELETE /api/calendar/events/{id}` - Удалить событие

---

## 🔧 Технологии

### Backend
- **FastAPI** 0.104.1 - Современный веб-фреймворк
- **SQLAlchemy** 2.0.23 - ORM для работы с БД
- **SQLite** - Легковесная СУБД
- **Pydantic** 2.5.0 - Валидация данных
- **JWT** - Токен-based аутентификация
- **Bcrypt** - Хеширование паролей
- **DeepSeek AI** - AI движок

### Frontend
- **Next.js** 14.0.4 - React фреймворк
- **React** 18.2.0 - UI библиотека
- **TypeScript** 5.3.3 - Типизированный JavaScript
- **TailwindCSS** 3.3.6 - CSS фреймворк
- **Lucide React** - Иконки
- **Axios** - HTTP клиент
- **Zustand** - State management

### Bot
- **aiomax** - MAKS SDK
- **AsyncIO** - Асинхронное программирование
- **AI Integration** - Интеграция с AI Core

### DevOps
- **Docker** - Контейнеризация
- **Docker Compose** - Оркестрация контейнеров

---

## 🔐 Безопасность

### Реализованные меры безопасности:

1. **JWT Authentication** - Токены с ограниченным временем жизни
2. **Bcrypt Password Hashing** - Надежное хеширование паролей
3. **CORS Protection** - Защита от межсайтовых запросов
4. **Pydantic Validation** - Валидация всех входных данных
5. **SQL Injection Protection** - Использование ORM
6. **Environment Variables** - Секреты хранятся в .env

### Рекомендации:

- Используйте сложный `SECRET_KEY` (64+ символов)
- Не храните `.env` файл в Git репозитории
- Регулярно обновляйте зависимости
- Используйте HTTPS в production
- Настройте файрвол для production

---

## 🐛 Устранение неполадок

### Проблема: Backend не запускается

**Ошибка:** `Port 8001 already in use`

**Решение:**
```bash
# Windows
netstat -ano | findstr :8001
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :8001
kill -9 <PID>
```

### Проблема: Frontend не может подключиться к Backend

**Проверьте:**
1. Backend запущен и доступен на http://localhost:8001
2. Переменная `NEXT_PUBLIC_API_URL` в `.env` корректна
3. CORS настроен правильно в Backend

### Проблема: Бот не отвечает

**Проверьте:**
1. `BOT_TOKEN` корректен
2. `DEEPSEEK_API_KEY` действителен
3. Backend доступен для бота
4. Логи бота: `docker-compose logs bot`

### Проблема: Docker контейнеры не запускаются

**Решение:**
```bash
# Пересоздать контейнеры
docker-compose down -v
docker-compose up -d --build

# Очистить все
docker system prune -a
```

### Проблема: База данных повреждена

**Решение:**
```bash
# Остановить сервисы
docker-compose down

# Удалить базу данных
rm Web/backend/eidos.db

# Запустить снова (БД создастся автоматически)
docker-compose up -d
```

---

## 📊 Мониторинг и логи

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f bot

# Последние N строк
docker-compose logs --tail=100 backend
```

### Health Check

```bash
# Проверка здоровья Backend
curl http://localhost:8001/health

# Ожидаемый ответ:
# {"status": "healthy", "version": "1.0.0"}
```

---

## 🎯 Roadmap

- [x] MAKS бот интеграция
- [x] Модули: Сон, Привычки, Финансы, Календарь
- [x] Веб-интерфейс с современным UI
- [x] Docker поддержка
- [x] JWT аутентификация
- [ ] Push уведомления в MAKS
- [ ] Мобильное приложение (React Native)
- [ ] Голосовые команды
- [ ] Интеграция с Google Calendar
- [ ] Экспорт данных
- [ ] Темная/светлая тема
- [ ] Мультиязычность
- [ ] PostgreSQL поддержка

---

## 🤝 Вклад в проект

Проект создан для хакатона Team 33.

Если хотите внести свой вклад:
1. Fork репозиторий
2. Создайте ветку (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📄 Лицензия

MIT License

Copyright (c) 2024 Team 33

---

## 📞 Поддержка

- **Документация:** [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)
- **GitHub Issues:** [Создать issue](https://github.com/your-username/EIDOS/issues)
- **Email:** support@eidos.app
- **Telegram:** @eidos_support

---

## 👥 Команда

**Team 33** - Хакатон 2024

---

## 🙏 Благодарности

- **DeepSeek AI** - за предоставление AI возможностей
- **MAKS** - за мессенджер платформу
- **FastAPI** - за отличный фреймворк
- **Next.js** - за React фреймворк
- **Все участники хакатона** - за вдохновение

---

<div align="center">
  <p>Made with ❤️ by Team 33</p>
  <p>
    <a href="#-содержание">⬆ Вернуться к началу</a>
  </p>
</div>
