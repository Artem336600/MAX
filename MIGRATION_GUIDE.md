# 🔄 Миграция базы данных

## Проблема
```
sqlite3.OperationalError: no such column: users.maks_id
```

В таблице `users` отсутствуют поля `maks_id` и `maks_username`.

## Решение

### Запустить миграцию:

```bash
cd Web/backend
python migrate.py
```

### Ожидаемый вывод:

```
==================================================
  МИГРАЦИЯ БАЗЫ ДАННЫХ
==================================================

🔄 Запуск миграции: Добавление MAKS полей
📁 База данных: C:\Users\Артём\OPTIMIZATION\Web\backend\eidos.db

Добавление поля maks_id...
✅ Поле maks_id добавлено
Добавление поля maks_username...
✅ Поле maks_username добавлено
Создание индексов...
✅ Индексы созданы

✅ Миграция успешно применена!

==================================================
  ГОТОВО!
==================================================
```

## Что добавляется

### Новые поля в таблице users:
- `maks_id INTEGER UNIQUE` - ID пользователя в MAKS
- `maks_username VARCHAR UNIQUE` - Логин для входа на сайт

### Индексы:
- `idx_users_maks_id` - для быстрого поиска по MAKS ID
- `idx_users_maks_username` - для быстрого поиска по логину

## После миграции

### Перезапустить бота:
```bash
cd chatbot
python bot_ai.py
```

### Протестировать:
1. MAKS → @t33_hakaton_bot → /start
2. Получить логин и пароль
3. Сайт → http://localhost:3000/maks-login
4. Войти

## Откат (если нужно)

⚠️ **Внимание:** Откат миграции для SQLite сложен, так как ALTER TABLE DROP COLUMN не поддерживается.

Рекомендуется:
1. Сделать бэкап БД перед миграцией
2. При необходимости восстановить из бэкапа

```bash
# Бэкап
cp eidos.db eidos.db.backup

# Восстановление
cp eidos.db.backup eidos.db
```

---

**Запустите миграцию и перезапустите бота!** 🚀
